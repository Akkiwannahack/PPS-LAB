def sum_of_digits(num):
    total = 0
    while num > 0:
        total += num % 10  # Get the last digit and add to total
        num //= 10  # Remove the last digit
    return total

# Input from user
num = int(input("num: "))
print("sum:", sum_of_digits(num))

